// I don't want to post my API keys, so here's where they'd go if they're there.
module.exports = {
  consumer_key:         '',
  consumer_secret:      '',
  access_token:         '',
  access_token_secret:  ''
}
